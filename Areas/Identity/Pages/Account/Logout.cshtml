@page
@model actividad.Areas.Identity.Pages.Account.LogoutModel
@{
    ViewData["Title"] = "Cerrar Sesión - AgroSoft";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">
            <div class="card shadow-sm mt-5 border-warning">
                <div class="card-body p-5 text-center">
                    <div class="mb-4">
                        <h2 class="card-title text-warning mb-2">🌾 AgroSoft</h2>
                        <p class="text-muted">Gestión Agropecuaria</p>
                    </div>
                    <hr class="border-warning" />
                    
                    <h3 class="mb-4">Cerrar Sesión</h3>
                    <p class="mb-4 text-muted">¿Estás seguro de que deseas salir del sistema de gestión agropecuaria?</p>
                    
                    <div class="d-grid gap-2">
                        <form id="logout-form" method="post" class="d-inline">
                            <button type="submit" class="btn btn-warning btn-lg">
                                <strong>Salir del Sistema</strong>
                            </button>
                        </form>
                        <a class="btn btn-outline-secondary btn-lg" asp-controller="Home" asp-action="Index">
                            Continuar trabajando
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

