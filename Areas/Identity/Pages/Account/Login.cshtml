@page
@model actividad.Areas.Identity.Pages.Account.LoginModel
@{
    ViewData["Title"] = "Acceso al Sistema AgroSoft";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">
            <div class="card shadow-sm mt-5 border-success">
                <div class="card-body p-5">
                    <div class="text-center mb-4">
                        <h2 class="card-title text-success mb-2">🌾 AgroSoft</h2>
                        <p class="text-muted">Gestión Agropecuaria</p>
                    </div>
                    <hr class="border-success" />
                    
                    <h3 class="text-center mb-4">Iniciar Sesión</h3>
                    <p class="text-center text-muted mb-4">Ingresa tus credenciales para acceder al sistema de gestión de cultivos y ganadería</p>
                    
                    <form id="login-form" method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3" role="alert">
                            @if (ViewData.ModelState.ErrorCount > 0)
                            {
                                <div class="alert alert-danger">
                                    <strong>Error de acceso:</strong> Las credenciales proporcionadas no son válidas. Verifica tu correo electrónico y contraseña.
                                </div>
                            }
                        </div>
                        
                        <div class="mb-3">
                            <label asp-for="Input.Email" class="form-label fw-bold">Correo Electrónico</label>
                            <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" placeholder="tu@correo.com" />
                            <span asp-validation-for="Input.Email" class="text-danger"></span>
                        </div>
                        
                        <div class="mb-3">
                            <label asp-for="Input.Password" class="form-label fw-bold">Contraseña</label>
                            <input asp-for="Input.Password" class="form-control" autocomplete="current-password" aria-required="true" placeholder="Ingresa tu contraseña" />
                            <span asp-validation-for="Input.Password" class="text-danger"></span>
                            <small class="form-text text-muted">Si olvidaste tu contraseña, contacta al administrador del sistema</small>
                        </div>
                        
                        <div class="mb-3 form-check">
                            <input asp-for="Input.RememberMe" class="form-check-input" />
                            <label asp-for="Input.RememberMe" class="form-check-label">
                                Mantener sesión activa
                            </label>
                        </div>
                        
                        <div class="d-grid">
                            <button type="submit" class="btn btn-success btn-lg">
                                <strong>🌱 Acceder al Sistema</strong>
                            </button>
                        </div>
                    </form>
                    
                    <hr class="my-4" />
                    
                    <div class="text-center">
                        <p class="mb-2 text-muted">¿Eres nuevo en AgroSoft?</p>
                        <a class="btn btn-outline-success" asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl">
                            Crear cuenta nueva
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
